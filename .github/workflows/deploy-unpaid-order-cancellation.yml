name: Deploy Unpaid Order Cancellation (superseded)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  superseded:
    runs-on: ubuntu-latest
    steps:
      - name: Use the verified order automation deployment
        run: |
          echo "This deployment is disabled. Use deploy-order-automations.yml on current main."
          echo "The replacement verifies all candidate hosts before promoting schedules."
          exit 1
