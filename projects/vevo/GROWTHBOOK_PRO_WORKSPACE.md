# VEVO GrowthBook Pro workspace contract

Status: A/A is configured and running only in staging; the unpublished Preview runtime, natural reconciliation, and same-population Meta/reporting audit are verified end to end. The Production foundation, dedicated reader, separate GrowthBook connection, `device_id`, assignment query, two fact tables, eight Starter-compatible metrics, collector runtime, localhost marker, healthy target, public-isolation boundary, and sanitized collector evidence are verified. The route is available without storefront traffic; Production allocation remains `0%`, GTM is unpublished, no Production experiment is running, and three paid-Pro p75 metrics remain pending.

Last reviewed: 2026-08-23

This is the operator handoff for the target GrowthBook Pro rollout. The authenticated workspace currently reports the `Starter` plan; no paid upgrade was accepted. The machine-readable source of truth is `growthbook_workspace.json`; the SQL pasted into GrowthBook must come from `growthbook_sql/` without manual edits.

## Creation order

1. **Completed:** authenticated organization is `Vevo`; the default project was safely reused and renamed `VEVO SK Web` (`vevo-sk-web`). The current UI does not expose a workspace region, so the manifest records it as unknown instead of claiming EU residency.
2. **Completed:** use the Starter default `staging` environment as the Preview alias and keep the default `production` environment. A custom environment named `Preview` is a paid feature in the observed UI. Production allocation stays `0%`.
3. **Completed for unpublished Preview:** Web SDK connection `VEVO SK Web Preview` exists only for `staging`, targets JavaScript SDK `1.7.0`, and includes draft experiment rules. Its client key is configured only in the isolated unpublished GTM workspace and is not committed. The controlled browser runtime has now fetched the staging rule and delivered an accepted exposure; no Production SDK connection exists.
4. **Completed:** string features `vevo-sk-aa-assignment` and `vevo-sk-product-cta-color` exist with default `control`, enabled for staging and disabled for production. Both experiments use 100% experiment traffic, a 50/50 split, exact string feature values, and staging-only rules; the A/A is now running as described in step 12, while CTA remains an unstarted draft. Nothing is enabled for Production.
5. **Completed:** the dedicated non-root ECS/Fargate collector passed the direct localhost host gate, the public route was added through a route-only change set, and run `32400301619` independently verified the active task/digest, exact CORS and route isolation, plus an unchanged raw-S3 snapshot after invalid probes.
6. **Completed:** run `32401658468` repeated the exact Fargate localhost gate and created `vevo-growthbook-preview-reader` with only the stack's `vevo-growthbook-readonly-preview` policy and one active access key. The key was delivered only as a one-day CMS-encrypted artifact, decrypted locally, and the cloud artifact was deleted immediately. No credential material is committed or logged.
7. **Completed:** `VEVO Preview Experiment Facts` (`ds_19g6mmt2c4dmn`) is connected to the exact Preview region, workgroup, catalog, database, and S3 results URL and is scoped only to project `VEVO SK Web`. GrowthBook's connection test passed, and the temporary local credential/private-key handoff was deleted immediately afterward.
8. **Completed and verified:** the data source has exactly one `device_id` identifier and one `VEVO consented devices` assignment query copied from `growthbook_sql/assignment.sql`. Recovery run `32442114254` proved the single expected synthetic assignment row through the exact read-only Athena workgroup, and the same query then passed directly in the GrowthBook UI.
9. **Completed:** protected run `32443149425` reused the exact synthetic A/A exposure/device, published one deterministic `lcp_ms=1300` event, reconciled it through the existing reporting runtime, and proved the exact raw, curated, and Athena identities after the Fargate localhost/marker hard gate. Production allocation remained `0%`.
10. **Completed:** `VEVO Device Outcomes v1` (`ftb_19g6mmt2dhrdi`) and `VEVO Performance Vitals v1` (`ftb_19g6mmt2e0otd`) were created from their version-controlled SQL, identified by `device_id`, and query-tested in GrowthBook with exactly one anonymous synthetic row each.
11. **Partially completed and query-verified:** all eight Starter-compatible outcome metrics were created and their exact GrowthBook IDs were read back into `growthbook_workspace.json`. A real GrowthBook metric-analysis run initially exposed the imported `integer = varchar(1)` mismatch on `client_error_observed`; all numeric outcome/quality columns were then corrected to GrowthBook Number, the three binary filters now render numeric `= 1`, and all eight metric analyses passed through the exact Preview Athena connection against one anonymous synthetic device. Outcome metrics use GrowthBook window `None` because the authoritative 24-hour and seven-day windows are already frozen upstream. The three performance p75 metrics remain intentionally uncreated: GrowthBook marks Quantile metrics as Pro, and no paid upgrade was authorized. After an authorized upgrade, create and query-test them with event-level quantile `0.75`, no grouping by experiment user, zeros retained, and a 24-hour conversion window.
12. **A/A configured and running in Preview/staging only:** feature `vevo-sk-aa-assignment` revision `2` is live only for `staging`, Production remains disabled, and A/A `vevo-sk-aa-001` is running at 100% experiment traffic with a 50/50 split. Its analysis uses `VEVO Preview Experiment Facts`, assignment table `VEVO consented devices`, add-to-cart within 24h as the diagnostic goal, six frozen outcome diagnostics as secondary metrics, and client-error device rate as the available guardrail. Default Bayesian statistics are used; CUPED, post-stratification, and activation metric remain off. CTA A/B `vevo-sk-product-cta-color-001` remains an unstarted draft with zero live rules until A/A passes and its final sample is recomputed and frozen.
13. **Completed as an unpublished draft only:** isolated GTM workspace `VEVO GrowthBook Preview` (`16`) contains exactly four new Preview tags and one new custom-event trigger. Its overview reads `5` added, `0` modified, and `0` deleted. Tag IDs are loader `44`, consent bridge `46`, add-to-cart bridge `47`, and purchase bridge `48`; custom trigger `45` observes `add_to_cart`. All three bridges were read back with the loader-before sequence and fail-closed behavior. The current runtime-populated artifact matches SHA-256 `f6b4972641efb7cc99d05b64b2c365c45eec20a6e5600ce9dade1dcaec694de1`; the same checksum is recorded in the workspace description. It was assembled only in the browser REPL, read back byte-for-byte before Save, and removed from the clipboard afterward. The client key is not committed. Nothing was submitted or published.
14. **Runtime Preview accepted through withdrawal/regrant:** after explicit user approval, Google Tag Assistant extension `26.216.2.45` was installed and its user-visible site access was confirmed as `all sites`. Comet's built-in blocker initially hid the GTM container and later blocked the GrowthBook feature host. Exact exceptions were added for `roy.flox.sk`, `vevo.flox.sk`, `www.vevo.sk`, and `www.roy.sk`; after the user restarted Comet, Tag Assistant connected to `vevo.sk`, found `GTM-5ZB5LFGB` plus both GA tags, and showed the unpublished `VEVO GrowthBook v1 - Loader - Preview` firing without browser warnings/errors. The post-restart diagnostic at `2026-08-21 14:13 Europe/Bratislava` independently showed three Google tags, the Preview loader fired once in the fresh window, and live `view_item_list`, consent-initialization, DOM-ready, and window-loaded events. Disabling stale feature caching for Preview had already exposed the live staging A/A rule immediately. With Analytical consent granted, the safe Preview markers reported `active` / `assigned`, numeric BiznisWeb consent values, bitwise consent `granted`, A/A variation `control`, and collector delivery `accepted`; a reload preserved the same assignment. Withdrawing Analytical consent removed the SDK, Web Vitals, exposure marker, and CTA style with zero console warnings/errors. Regranting Analytical consent restored the same accepted/stable runtime, while every optional non-Analytical category remained denied. CTA has zero live rules and no CTA style was applied. Production remains disabled and GTM remains unpublished.
15. **Controlled real Preview partition reconciled:** protected run `32453223068` on merged main commit `521472cac27b779f6bd1b969cadd1e4dfd8870fd` passed the Fargate host gate and a separate reconciliation task using immutable reporting digest `sha256:194d97bc159e59678cf184cdad3c33c0f5b2ddf501fa31d1d3422c6a7b5d2f68`. The bounded `2026-08-21` partition reconciled `23` raw events into `5` device facts and `11` performance facts, with `0` transaction and `0` order facts. Generated/published counts matched, the exact synthetic identity was preserved without logging browser device IDs, and Athena assignment query `ef981af5-3c3f-4d32-813a-a546be77b79b` completed the raw/curated/reporting/Athena identity chain. Production allocation remained `0%`.
16. **Recurring reconciliation deployed; one-shot and second natural retention recovery verified:** protected run `32459100570` deployed main commit `4e4443beea2a2da466d80f781199bd4684dfac0c` on immutable digest `sha256:cabba3b0bd57f6be322f3a5ff62f0327c7cf8e7bb2b6b5e78686305339fdd041`. The hard gate ran task `29d5e5d3fed349d79dec1384f5aff32a` at private IP `172.31.25.184`, service `vevo-growthbook-reconcile-preview`, path `/app`; the one-shot task `668418a08c504e078288f407df44a15e` at `172.31.18.86` passed generated/published parity for the latest 40 complete UTC partitions. The exact AWS schedule remains enabled on task definition `:4` at `cron(30 3 * * ? *)`, timezone `Europe/Bratislava`, with its retained encrypted DLQ and three alarms verified; source schedule `vevo-daily-report-email` remains unchanged. The first genuine run succeeded but expired from ECS before full read-back. The second genuine run remained inspectable as task `e62569dcfb7a44d38f386e25ca34b272`; read-only verifier run `32611298457` on exact main commit `be2d13f509c2438b1d569003417356c5b1c0ce75` passed at `2026-08-23T01:50:03Z`. It proved Fargate instance identity `N/A`, private IP `172.31.13.165`, service `vevo-growthbook-reconcile-preview`, path `/app`, task definition `:4`, exact immutable digest, exit `0`, exact CloudWatch task binding, Scheduler CloudTrail execution, source schedule, clear alarms, empty DLQ, and generated/published parity across 40 complete UTC partitions (`28` raw events, `5` device facts, `15` performance facts, `2` quality reports). Sanitized artifact `vevo-growthbook-natural-reconciliation-evidence-32611298457` was independently downloaded and recorded with SHA-256 `69e5ab22a61a464abf3a9c7c354099f7c1d037a1da58b1fba41fb12ac50222b1`; it contains no credentials, raw AWS payloads, customer or order data. The temporary GitHub one-time cron was removed after evidence capture; manual dispatch remains fail-closed. Production allocation remains `0%`, reader provisioning and GrowthBook cloning remain disabled, while the reviewed route-disabled Production foundation gate is now open.
17. **Same-population and Meta dimension audit completed:** PR `#316` fixed the proven pre-`main()` import boundary as merge commit `9ef741b328cb5709f1e3e7e78c2f4b7afeadc066`; ECR build `32463854583` verified immutable digest `sha256:95efe5fffa2f4a3c7ded6c710697b0d5f6f6b45fbc525ad6a848a069753234ef`. Protected run `32464046045` passed its localhost/marker hard gate on Fargate task `cd74ee3a3b5d43e999c45b15b0fdec1`, IP `172.31.16.54`, service `vevo-growthbook-meta-audit-preview`, path `/app`, and task definition `:3`. The read-only Meta task `3b1e00828825416da1f5c1422e6d1cac` at `172.31.19.87` exited `0`. Across the 30 complete UTC days `2026-07-22..2026-08-20`, 19 traffic ads in three campaigns and three ad sets produced 2,210 clicks and EUR 523.13 spend. `utm_source`, `utm_medium`, and `utm_id` covered 100% of clicks/spend, while `utm_content`, `meta_adset_id`, `meta_placement`, and the complete six-field contract covered 0%; no forbidden click-identifier parameter was found. Athena then proved exact assignment/outcome parity: five rows and five unique keys on each side, zero duplicates, zero anti-join misses, three rows with complete valid Meta dimensions, and zero invalid dimension rows (queries `0acd8e02-d081-4cec-a85e-71034321f8de` and `5ef71971-9ea2-4511-8d49-517a1c00318a`). No Meta delivery, GTM, GrowthBook allocation, BiznisWeb, order, price, cart, or checkout mutation occurred.
18. Before A/A Production traffic, clone the verified Preview data-source/fact/metric objects onto the separate Production Athena database and workgroup. Never repoint the Preview connection in place.
19. **Production foundation preflight passed, not deployed:** PR `#321` merged the exact `decision_gates` fix as `f50e5712b039b991e2bc986552af8d8a54a6d551`. Protected run `32466708456` passed the full local contract and read-only AWS inspection. It verified account `919341186960`, region `eu-central-1`, healthy Preview task `4a2cdbe240794f439b68ad674f9bb2d6`, private IP `172.31.34.243`, service `vevo-growthbook-collector-preview`, path `/app`, and image digest `sha256:9478acd98a8caf06374b018c563ee51fa896b9cc92148238579f04aa28a134e1`. The Production stack is absent; the planned service remains route-disabled with an empty registry and `0%` allocation. No Production stack, route, credentials, GrowthBook data source, traffic, or external mutation was created.
20. **Production foundation stack created route-disabled; live state and schema-v2 evidence verified:** the main-only workflow reuses the exact host-verified Preview image digest and permits only an absent-stack `CREATE` change set with the reviewed route-disabled resource allowlist. Initial dispatch `32611782561` stopped before CREATE because ECS omitted the pre-deploy task's optional duplicate `logStreamName`; no stack was created. After the bounded task-definition log resolver merged, run `32612205628` passed the pre-CREATE host gate on task `23516a043fb5403483db0503128ec21a`, IP `172.31.34.40`, service `vevo-growthbook-collector-production`, path `/app`, and completed the reviewed route-disabled CREATE. The deployed host gate then passed on task `9cb1d826dd244eff9fb612cb76bfac03`, IP `172.31.19.203`, task definition `:1`, immutable digest, localhost health, and marker. The final read-only check reported the new bucket nonempty and failed closed before artifact upload. PR `#351` then added a no-mutation diagnostic; independent runs `32612900470` and `32612957004` both proved the bucket stably empty (`0` raw, `0` Athena, `0` unexpected), with no keys or content exposed. PR `#352` merged the one-time schema-v2 recovery as `4aefe8b43a3c17756ab127303e2c59bce71be546`; run `32613802790` bound the original CREATE metadata and all 31 live resources, passed a fresh localhost/marker gate on task `2ec01f9200724e1997dbf3e7c989724e`, IP `172.31.12.218`, and re-proved live task `acaf0e0a472b416d8a9df927f06d1943`, IP `172.31.42.150`, healthy target, exact nonpublic AES256 empty bucket with no multipart uploads, IAM reader `NoSuchEntity`, zero routes, external `404`, and `0%` allocation. Artifact `vevo-growthbook-production-foundation-evidence-32613802790` was independently downloaded and hash-verified as `bfc6ea2f0033d6ff8ce07cf3131ce114e9e6ed4f64afb86141ad12d44e259828`; the offline recorder embedded it in the manifest and opened only the separately reviewed reader gate. GrowthBook cloning, GTM publishing, Meta Ads, and BiznisWeb commerce remain blocked.
21. **Separate Production reader verified; clone contract opened but not yet executed:** the main-only Production reader workflow cannot reach AWS credentials until the natural verifier passes, the route-disabled Production foundation artifact is canonical, embedded through its offline recorder, bound to independently read workflow run ID/main commit and SHA-256, recorded as `CREATE_COMPLETE`, and `reader_provisioning_allowed=true`. The Production registry must still be empty, allocation `0%`, GTM unpublished, the foundation redeployment gates closed, and GrowthBook cloning disabled. The reader local gate revalidates the entire artifact and natural-evidence provenance before AWS credentials. It then rechecks the exact Production task/IP/service/`/app`, immutable digest, direct localhost health/marker, zero API routes, a privacy-safe empty-bucket summary, zero incomplete multipart uploads, exact IAM `NoSuchEntity` both before the host gate and immediately before user creation, and the exact curated-only Athena policy before creating `vevo-growthbook-production-reader` under `/vevo/growthbook/production/`. Its task-definition-bound log resolver accepts only an absent optional ECS duplicate or an exact noncontradictory stream. Both failure cleanup paths require the current run's creation marker before revoking anything, so a raced or pre-existing identity is never deleted. Dispatch `32614216513` failed before the host gate and IAM creation on the superseded direct `KeyCount` assertion; diagnostic `32614283239` stopped locally on its obsolete one-time pre-evidence gate and made no AWS request. PR `#354` merged the correction as `79f1eb4b1b29bb65efbdbe310b0033e1a5a1f594`. Run `32614706434` then passed all gates, including bucket `0/0/0/0`, live task `acaf0e0a472b416d8a9df927f06d1943` at `172.31.42.150`, and localhost task `2c33ccdc1a1148758fe3c5fac162fb11` at `172.31.28.57`, before creating exactly one dedicated reader/key. Artifact `9486585740` contains only the one-day CMS-encrypted credentials and downloaded with SHA-256 `2f8c0a2941ce5c345fc518b2e562cabcc50d141ace9de71b61bffab74a7cd59f`; artifact `9486585888` is the distinct canonical 14-day sanitized evidence with SHA-256 `1715f2b41a1bfd1d58524bdbad8369afc63b76a30d145f959a9cc742370b01d7`. The machine-readable clone contract now permits the separately reviewed UI clone but freezes the Preview source data source, two fact-table IDs, eight verified Starter-compatible metric IDs, target IDs as `null`, and the creation order. It explicitly forbids repointing Preview. No Production GrowthBook object has yet been created, and the three p75 metrics remain blocked until a paid Pro upgrade is explicitly authorized.
22. **A/A acceptance decision prepared offline:** `growthbook_aa_acceptance.json` freezes the seven-full-local-day, 1,000-device, 200-page-load-per-arm, 48–52%, SRM, count-parity, duplicate, exact-order-join, privacy, consent, Meta-dimension, commerce, rollback, and performance thresholds. `scripts/evaluate_growthbook_aa.py` accepts only an exact aggregate schema, independently recomputes the derived statistics, rejects inconsistent/non-finite/extended evidence, and returns only `PASS`, `FAIL`, or `NOT_READY` with `winner_calls_allowed=false`. It has no AWS, GrowthBook, GTM, Meta Ads, BiznisWeb, or network client. A later protected read-only evidence workflow must populate the snapshot from authoritative Production sources; a locally assembled snapshot is never sufficient to activate traffic or start CTA A/B.
23. **Collector receipt evidence prepared, not deployed:** after each successful immutable raw-event persistence, the source collector emits one schema-v1 `VEVO_GROWTHBOOK_COLLECTOR_RECEIPT` marker containing only `accepted=true` and the boolean `duplicate` state. It contains no event/device identity, URL, campaign dimension, raw payload, order identity, or customer data; logging failure cannot change an already persisted event into a failed storefront request. `scripts/summarize_growthbook_receipts.py` validates a complete bounded CloudWatch export offline and writes only received, unique-accepted, and duplicate aggregate counts for the A/A evaluator. The raw CloudWatch JSON is temporary local input and must never be committed or uploaded as evidence. The deployed Preview collector remains on its previous image and no Production collector exists until the later reviewed foundation/deploy gates authorize them.
24. **Protected A/A snapshot assembler prepared and hard-disabled:** `growthbook_aa_snapshot.json` keeps `snapshot_build_allowed=false` and both required Production evidence identities null. The main-only workflow `build-vevo-growthbook-production-aa-snapshot.yml` has no AWS credentials/API calls and reads exactly two successful, independently hash/run/commit-bound GitHub artifacts: one automated aggregate Production component and one manual GrowthBook/Tag Assistant/commerce QA component. `scripts/assemble_growthbook_aa_snapshot.py` requires canonical JSON bytes, exact schemas, identical windows, private Fargate runtime identity, PII/raw-data exclusion flags, and independently supplied provenance; runtime and provenance fields are validated but omitted from the evaluator snapshot. It then invokes the offline A/A validator and uploads only the exact aggregate snapshot and `PASS`/`FAIL`/`NOT_READY` decision, always with `winner_calls_allowed=false`. The workflow cannot start or stop traffic, query AWS, mutate GrowthBook/GTM/Meta/BiznisWeb, activate CTA, or run until the Production foundation, reader, clone, 100% A/A allocation, and both sanitized evidence artifacts are separately verified and recorded.
25. **Manual Production A/A QA evidence producer prepared and hard-disabled:** `verify-vevo-growthbook-production-aa-manual-qa.yml` cannot run until its manifest gate is opened by a reviewed change after the actual GrowthBook, Tag Assistant, desktop/mobile, consent accept/reject/withdrawal, rollback, purchase-duplication, and commerce QA. The canonical observation file is intentionally absent, its SHA-256 is null, and `producer_allowed=false`. When those facts are genuinely observed, the offline builder validates the exact no-PII/no-unplanned-mutation schema and frozen 100% A/A-only allocation, injects the successful main-branch workflow run ID and commit, and uploads only `vevo-growthbook-aa-manual-qa-evidence.json`. The workflow has no AWS credentials, browser automation, network client, GrowthBook/GTM/Meta/BiznisWeb mutation, winner, or CTA activation path.
26. **Automated Production A/A evidence producer prepared and hard-disabled:** `collect-vevo-growthbook-production-aa-evidence.yml` stops before AWS credentials while `producer_allowed=false`, the UTC window is absent, and no exact reporting-quality S3 key or SHA-256 is recorded. A later reviewed change must freeze all three before the workflow can read the exact Production stack/service/task/image, the previously localhost-gated foundation identity, Glue schemas, bounded collector receipts, the hash-bound reporting quality file, one aggregate-only Athena result, and conservative API consent rejects. Raw CloudWatch/AWS responses and query files exist only under the isolated runner temporary directory and are deleted before the workflow uploads exactly one canonical no-identity evidence file. The offline builder has no AWS/network client, and the workflow contains no deploy, IAM/resource write, GrowthBook, GTM, Meta Ads, BiznisWeb, winner, or CTA activation path. The same review also corrected the manual producer's future experiment lookup to the authoritative `tracking_key`; the closed gate meant the defect never reached Production.
27. **Production reader verified and credential handoff destroyed:** workflow run `32614706434` provisioned the curated-only reader and produced canonical secret-free evidence bound to main commit `79f1eb4b1b29bb65efbdbe310b0033e1a5a1f594`. The source manifest records the exact evidence SHA-256 and runtime identity. After the authenticated GrowthBook connection passed, the one-time plaintext, local RSA key/certificate, encrypted credential file, and one-day GitHub credential artifact were destroyed; only sanitized evidence remains. Production allocation is still `0%` and GTM is unpublished.
28. **Production GrowthBook clone evidence recorded and activation still hard-disabled:** `GROWTHBOOK_PRODUCTION_CLONE_RUNBOOK.md` freezes the separate Athena source, `device_id` assignment query, two fact tables, and eight Starter-compatible metrics. `scripts/record_growthbook_production_clone_evidence.py` has no GrowthBook/AWS/browser/network client and can record only canonical sanitized IDs, repository-derived hashes, zero/one-row counts, and safety booleans after reader validation. The recorded observation closes `clone_allowed`, sets `mutation_status=created_and_query_verified`, and moves the next gate to a separately reviewed Production A/A activation; it cannot publish GTM, start an experiment, change Meta Ads, or move allocation above `0%`.
29. **First CTA A/B decision evaluator prepared and fail-closed:** `growthbook_cta_decision_contract.json` freezes the deterministic first-`N` cohort, one final fixed-horizon primary look, two-sided `5%` primary test, 14/42-day assignment limits, 14-day post-stop maturity, separately monitored early safety stops, data-quality thresholds, 10% relative CM1 non-inferiority boundary, mature cancellation/refund checks, and the same performance/client-error thresholds used by A/A. `scripts/evaluate_growthbook_cta.py` accepts only the exact aggregate no-identity schema, independently recomputes SRM/statistics/intervals, validates the canonical frozen sample-plan SHA-256 and A/A snapshot SHA-256, and returns only `WIN`, `LOSE`, `INCONCLUSIVE`, or `NOT_READY` with `automatic_mutation_allowed=false`. `growthbook_cta_lifecycle_reconciliation.json` is a separate refund/credit-note value gate and remains pending/false. `scripts/record_growthbook_cta_lifecycle_reconciliation.py` can open it only from canonical identity-free observation bytes plus an independently supplied SHA-256, exact cent-level CM1 parity, a mature lifecycle case, matching lifecycle counts, and the exact curated reporting-quality object key/hash; the evaluator then requires the checked-in observation file and recomputes its canonical SHA-256. Neither tool has an AWS, GrowthBook, GTM, Meta Ads, BiznisWeb, browser, network, traffic, price, cart, checkout, or order mutation client.
30. **Production clone verified complete with GrowthBook 5.0.1 schema recovery:** the authenticated UI created data source `ds_19g6mmt5stlp6`, retained only `device_id` plus `VEVO consented devices`, and passed the curated-only Production Athena connection. Device Outcomes `ftb_19g6mmt5tg48t` and Performance Vitals `ftb_19g6lmt5ueyhu` store the exact merged Production SQL. Each GrowthBook UI test returned the one wildcard-only metadata probe; separate unsaved curated-only tests returned zero rows without SQL error. All numeric columns were manually typed and read back. Exactly eight Starter-compatible metrics were created with matching type, filter, aggregation, goal, and disabled GrowthBook window; the three p75 metrics remain absent. Preview IDs and exact probe-free SQL were read back unchanged. Canonical observation SHA-256 `b2f96b7047321f11da4f00c7886c4b9422d7759428534f8fd5534ee1299f2030` records only sanitized IDs, hashes, counts, and safety booleans. No experiment is running, GTM remains unpublished, and allocation remains `0%`.

## Exact analytical behavior

- Assignment and conversion identity is the anonymous random `device_id`; no identifier join table exists.
- GrowthBook queries curated anonymous facts only. It cannot query raw events, orders, customers, invoices, or unrelated reporting data.
- The assignment table intentionally contains one eligible first-exposure row per experiment/device. Cross-variation contamination is detected from retained raw exposures by the reporting builder and contaminated devices are excluded before the table reaches GrowthBook.
- Non-buyers remain present with zero revenue and zero CM1, so GrowthBook Mean metrics correctly divide by every eligible exposed device.
- Purchase, revenue, and CM1 are computed by the authoritative BiznisWeb/reporting join within seven days. Browser money is never accepted.
- Cancellation and refund ratios cannot be used for a final decision until the 14-day maturity gate passes.
- Meta campaign, ad-set, ad, and placement IDs are analysis dimensions only; GrowthBook, not Meta, owns the 50/50 randomization.
- A/A results never produce a winner. They validate assignment, SRM, joins, privacy, performance, and agreement with reporting.

## Current blockers

- The authenticated GrowthBook workspace is on Starter. The observed upgrade flow requires a paid Pro upgrade; it was cancelled without accepting a charge.
- `staging` is the Preview alias until a paid plan is explicitly authorized and a custom `Preview` environment is genuinely needed.
- The controlled `2026-08-21` Preview partition is reconciled and identity-verified through the exact GrowthBook Athena workgroup. The same-population audit proves exact assignment/outcome agreement. The recurring schedule, protected one-shot, and second natural run are fully verified and recorded through canonical schema-v2 evidence from run `32611298457`.
- The GrowthBook data source, assignment query, both fact tables, and all eight Starter-compatible outcome metrics are query-verified. The LCP, INP, and CLS p75 Quantile metrics are blocked until a paid Pro upgrade is explicitly authorized; their GrowthBook IDs remain `null` rather than claiming objects that do not exist.
- GTM workspace `VEVO GrowthBook Preview` remains an unpublished draft with five added and no modified/deleted objects. Tag Assistant attaches, the no-Analytical fail-closed gate passes, and the Analytical-only staging payload, stable assignment, accepted collector delivery, withdrawal cleanup, regrant, controlled-partition reconciliation, and same-population reporting agreement are verified. The remaining operational gate is the natural-run retention recovery. Existing live ads stay unchanged during A/A; the full six-field Meta parameter contract must be used for every newly created or otherwise edited ad before it becomes eligible for dimension-complete analysis.
- Production feature allocation and the collector Production registry remain hard-disabled.

## Next exact step

Merge the exact collector evidence through reviewed CI. Then prepare the separate Production GrowthBook SDK connection, A/A experiment, feature rule, and GTM tags only at `0%` allocation in an unpublished workspace. Do not click GTM `Submit`, start A/A/CTA, change Meta Ads, accept a paid Pro upgrade, create the three p75 metrics, repoint Preview, or send storefront traffic during zero-allocation preparation.

After Production A/A has genuinely run, evaluate only the protected aggregate snapshot and require `PASS` explicitly:

```text
python scripts/evaluate_growthbook_aa.py --snapshot <downloaded-protected-aa-snapshot.json> --output <temporary-aa-decision.json> --require-pass
```

`NOT_READY` means keep collecting without changing the hypothesis. `FAIL` means stop A/A traffic, execute the tested rollback, diagnose the failed gates, and restart the full A/A only after a versioned fix. A/A never yields a winner and never starts the CTA A/B automatically.

Post-success evidence handoff template; placeholders must come from the successful GitHub run metadata, not from the artifact itself:

```text
python scripts/record_growthbook_natural_evidence.py --evidence <downloaded-artifact>/vevo-growthbook-natural-reconciliation-evidence.json --workspace projects/vevo/growthbook_workspace.json --output projects/vevo/growthbook_workspace.json --expected-workflow-run-id <run-id> --expected-main-commit <head-sha>
python scripts/validate_growthbook_workspace.py
python -m unittest tests.test_growthbook_natural_evidence_recorder tests.test_growthbook_workspace tests.test_growthbook_production_foundation_workflow
git diff --check
```

Post-foundation evidence handoff template; use only the successful evidence-producing foundation or schema-v2 recovery run's independently read metadata (the original CREATE provenance remains separately embedded and validated inside schema v2):

```text
python scripts/record_growthbook_foundation_evidence.py --evidence <downloaded-artifact>/vevo-growthbook-production-foundation-evidence.json --workspace projects/vevo/growthbook_workspace.json --output projects/vevo/growthbook_workspace.json --expected-workflow-run-id <run-id> --expected-main-commit <head-sha>
python scripts/validate_growthbook_workspace.py
python -m unittest tests.test_growthbook_foundation_evidence_recorder tests.test_growthbook_workspace tests.test_growthbook_production_reader_workflow
git diff --check
```

Post-reader evidence handoff template; use only a successful reader run's independently read metadata and keep the CMS credential artifact outside Git:

```text
python scripts/record_growthbook_production_reader_evidence.py record --evidence <downloaded-artifact>/vevo-growthbook-production-reader-evidence.json --workspace projects/vevo/growthbook_workspace.json --output projects/vevo/growthbook_workspace.json --expected-workflow-run-id <run-id> --expected-main-commit <head-sha>
python scripts/validate_growthbook_workspace.py
python -m unittest tests.test_growthbook_production_reader_evidence_recorder tests.test_growthbook_workspace tests.test_growthbook_production_reader_workflow
git diff --check
```

The GrowthBook Web SDK returns the actual string feature value in `ExperimentResult.value`; its `key` is a variation tracking key that defaults to a numeric string such as `"0"` or `"1"`. The storefront therefore validates and stores `result.value` (`control`, `variant`, or `brand_contrast`) and uses numeric metadata only as a fail-closed fallback. See the official [GrowthBook JavaScript SDK source](https://github.com/growthbook/growthbook/blob/main/packages/sdk-js/src/types/growthbook.ts) and [feature implementation guide](https://docs.growthbook.io/lib/js#tracking-callback).

Validate this contract before any UI entry:

```text
python scripts/validate_growthbook_workspace.py
python -m unittest tests.test_growthbook_workspace
```

Official implementation references:

- <https://docs.growthbook.io/app/datasources>
- <https://docs.growthbook.io/app/metrics>
- <https://docs.growthbook.io/app/sql-templates>
- <https://docs.growthbook.io/warehouses/athena>
